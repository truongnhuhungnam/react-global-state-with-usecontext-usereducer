const initState = {
    todos: [],
    todoInput: "",
};

const reducer = (state, action) => {
    switch (action.type) {
        default:
            throw new Error("Invalid action type");
    }
};

export { initState };
export default reducer;
